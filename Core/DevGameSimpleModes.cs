namespace MGT2AssistantButton.Core
{
    public enum TargetGroupMode { Best }
    public enum GenreMode { Best }
    public enum ThemeMode { Best }
    public enum GameNameMode { Real }
    public enum EngineMode { Best }
    public enum AntiCheatMode { Best }
    public enum CopyProtectMode { Best }
    public enum SliderMode { Optimal }
}
